## 1. Establish the canonical entity package

- [x] 1.1 Create `src/multica_py/entities/`; move `_BoundEntity`, `_RuntimeHolder`, `_runtime_state`, `_is_mapping`, `_normalize_entity_id`, and their other private support functions unchanged from `models/_bound.py` into `entities/_base.py`; update direct users to the new private path and verify bound serialization/detach/equality tests remain green.
- [x] 1.2 Move the leaf bound definitions `Label`, `Comment`, `CommentThread`, and `Skill` into `entities/labels.py`, `entities/comments.py`, and `entities/skills.py`; leave their former resource modules defining resource services and re-exporting the identical canonical classes through their existing `__all__` entries.
- [x] 1.3 Move `Issue` and `TaskRun` into `entities/issues.py` without changing fields, normalization, serialization, relation names, actions, annotations, or errors; make `IssueResource` and `IssueCommentResource` return/bind the canonical entity classes.
- [x] 1.4 Replace free issue-page/comment adapters that combine command construction or wire adaptation with private adapters on the owning resource service; keep any helper that only updates relation state from already decoded values in the entity or neutral layer. Have `Issue`, `TaskRun`, and later issue-list relations call resource-owned adapters through their bound client so `entities/issues.py` does not import `resources.*` or construct/decode a command plan.
- [x] 1.5 Move `Agent`, `Project`, and `Squad` into `entities/agents.py`, `entities/projects.py`, and `entities/squads.py`; keep pure relation-state helpers entity/neutral, redirect command construction and wire adaptation through the originating client's private resource adapters, and preserve cache invalidation and command-preview behavior.
- [x] 1.6 Move `Autopilot` and `AutopilotRun` into `entities/autopilots.py`; keep JSON-value normalization and seeded relation-state behavior intact, and move history/run command construction and wire adaptation behind private `AutopilotResource` adapters used through the bound client.
- [x] 1.7 Move `Workspace` and `WorkspaceMember` into `entities/workspaces.py`; preserve derived workspace scoping and every members/agents/skills/projects/labels/repositories/runtimes/squads/issues/autopilots relation, keeping pure relation state in entity/neutral helpers and delegating command/wire adaptation to owning resources.
- [x] 1.8 Add `entities/__init__.py` exporting exactly the 13 canonical public classes, excluding `_BoundEntity`; update `multica_py.__init__` to import entities from the canonical package while preserving every existing root entity name and class identity.

## 2. Normalize imports and enforce architecture boundaries

- [x] 2.1 Redirect entity references in resources, models, `client.py`, `_internal/wire_models.py`, `_internal/decoders.py`, tests, and typing-only blocks to `multica_py.entities`; retain sibling resource imports only for actual nested-service composition, not to name or construct an entity.
- [x] 2.2 Preserve former `multica_py.resources.<domain>` entity imports as direct compatibility re-exports and assert each is identical to both the package-root and `multica_py.entities` class; do not add wrapper classes or duplicate `msgspec.Struct` definitions.
- [x] 2.3 Delete `models/_bound.py` after `rg 'models\._bound' src tests` and an import scan show zero references; verify neither `multica_py.models` nor `multica_py.entities.__all__` exposes `_BoundEntity`.
- [x] 2.4 Add a table-driven contract test for the exact 13-class inventory, canonical defining modules, `_BoundEntity` inheritance, root/entity/resource alias identity, and absence of bound class definitions under `resources` and `models`.
- [x] 2.5 Add a focused dependency/import contract that asserts pure relation-state helpers have no client, `Command`/argv, transport, or raw-wire-decoding dependency and may reside only in entity/neutral layers, while command-construction and wire-adaptation adapters are private methods on owning resources called through the bound client; reject canonical entity imports routed through sibling resource modules while allowing documented nested-resource composition, and import every entity, resource, `_internal.wire_models`, and `_internal.decoders` module in fresh interpreter processes to prove order-independent, cycle-free imports.
- [x] 2.6 Run the existing bound-entity, issue/agent/project/squad/workspace/autopilot relation, operation-routing, public-surface, and public-invariant tests; fix only relocation regressions and confirm commands, values, exceptions, and annotations are unchanged apart from defining-module paths.

## 3. Add the public structured process result

- [x] 3.1 Add frozen closed `ProcessResult` in `src/multica_py/process.py` with exact typed fields `argv`, `exit_code`, `stdout`, and `stderr`, plus computed `ok` and `failed` properties; use the repository's immutable `msgspec.Struct` model convention and no domain payload or duration field.
- [x] 3.2 Add `ProcessOutputModeError(MulticaError)` in `exceptions.py` with actionable messages that identify the current output state and requested consumer; export it and `ProcessResult` from the package root and update the public export/type contract tables.
- [x] 3.3 Add private unclaimed/buffered/streaming/discarded output state and a cached `ProcessResult` to `ManagedProcess`; make stream generators claim streaming mode on first iteration rather than generator creation, and reject every buffered/streaming switch before another pipe read.
- [x] 3.4 Implement `ManagedProcess.result(timeout=None)` with `Popen.communicate(timeout=...)` so stdout and stderr drain concurrently; decode complete bytes as strict UTF-8, cache one result before finalization, return the identical cached object on repeats, and finalize even if post-completion decoding/result construction fails.
- [x] 3.5 Change `ManagedProcess.wait(timeout=None)` to return `self.result(timeout).exit_code`; preserve the existing `datetime.timedelta` input and built-in `TimeoutError` convention while eliminating the pipe-fill deadlock and retaining output for later `result()` access.
- [x] 3.6 On `TimeoutExpired`, retain buffered ownership without caching, closing pipes, finalizing, or releasing the semaphore so `result()`/`wait()` can retry; keep `terminate()` and `kill()` signal-only so a caller can collect trailing output and the real exit code afterward.
- [x] 3.7 Keep `close()`, context exit, and destructor fallback as the explicit discard path with terminate/wait/kill escalation and exactly-once `_finalize()`; mark discarded output so later result or iterator access raises `ProcessOutputModeError` without another process or semaphore operation.

## 4. Verify managed-process behavior

- [x] 4.1 Extend the table-driven unit process cases for zero/nonzero results, exact argv/stdout/stderr, `ok`/`failed`, immutable fields, repeated-result object identity, wait-then-result, and no extra wait/read/finalize/semaphore release.
- [x] 4.2 Add mode-transition unit cases proving generator creation alone does not claim streaming, first iteration does, stream-then-result/wait and buffered-then-stream raise `ProcessOutputModeError` before pipe access, and access after discard raises without re-finalizing.
- [x] 4.3 Add timeout/recovery unit cases proving the first timeout caches nothing and releases no capacity, the retry returns complete unduplicated output, and decoder failure after completed communication still finalizes exactly once.
- [x] 4.4 Extend `tests/fixtures/child_process.py` and `tests/component/test_process_contract.py` with real-process cases for stdout-only, stderr-only, interleaved output larger than pipe capacity, nonzero exit, wait-then-result, output both before and after a retryable timeout, terminate/kill followed by result, and explicit close; keep them in the existing serial/process-marked component module.
- [x] 4.5 Re-run the existing direct streaming and semaphore tests to prove incremental line/newline behavior and process-limit accounting remain unchanged; verify ordinary resource/transport commands still return domain models or raise their existing classified exceptions rather than `ProcessResult`.

## 5. Documentation and compatibility coverage

- [x] 5.1 Update `docs/api.md` to document `multica_py.entities`, canonical and compatibility import paths, the entity/resource responsibility split, `ProcessResult`, `ManagedProcess.result()`, cached result identity, and `ProcessOutputModeError`.
- [x] 5.2 Update `docs/service-usage.md` with one finite-output buffered example using `result()` and one long-running context-managed streaming example; state that `wait()` buffers output, streaming and buffered modes cannot mix, and finite buffering may use unbounded memory.
- [x] 5.3 Update `docs/migration.md` to state that entity root/resource imports and behavior remain compatible while defining module paths move, and explain how legacy stream-then-wait code must choose either buffered `result()`/`wait()` or context-managed direct streaming.
- [x] 5.4 Update `tests/contract/test_bound_public_docs.py`, `tests/contract/test_bound_public_surface.py`, and `tests/contract/test_public_invariants.py` to pin the new exports/documentation without widening the package root beyond `ProcessResult`, `ProcessOutputModeError`, and the already-supported categories.

## 6. Final verification

- [x] 6.1 Run the focused entity suites: `uv run pytest -m "not live" tests/unit/resources/test_bound_entity.py tests/unit/resources/test_issue_relations.py tests/unit/resources/test_agent_skill_squad_relations.py tests/unit/resources/test_project_relations.py tests/unit/resources/test_workspace_relations.py tests/unit/resources/test_workspace_relations_extra.py tests/unit/resources/test_autopilot_relations.py tests/contract/test_bound_public_surface.py tests/contract/test_public_invariants.py`.
- [x] 6.2 Run the focused process suites: `uv run pytest -m "not live" tests/unit/test_process_lifecycle.py tests/unit/test_process_state.py tests/component/test_process_contract.py tests/component/test_streaming_commands.py tests/component/test_service_safety.py`.
- [x] 6.3 Run `uv run pytest -m "not live"` and `uv run pytest -m "not live" --collect-only`; confirm the full offline suite passes and no `tests/live/*` node is collected.
- [x] 6.4 Run `uv run mypy src`, `uv run mypy tests`, `uv run ruff check`, and `uv run ruff format --check`; fix all failures without adding public `Any`, test-only type escapes, or new dependencies.
- [x] 6.5 Run `uv run openspec validate extract-entities-and-add-process-results` and `uv run openspec validate --specs`; confirm the delta applies cleanly to `entity-package-boundaries`, `subprocess-transport`, and `sdk-surface`.
- [x] 6.6 Run `uv run pytest -m "not live" tests/unit/resources/test_operations.py::test_discovered_public_methods`; confirm the exact discovered public resource-method set is unchanged by entity relocation and process additions, with no method added or removed.
- [x] 6.7 Inspect `git diff --check`, `rg 'models\._bound' src tests`, the resource/entity import graph, and `git status`; confirm only intended implementation, test, documentation, and OpenSpec files changed and no generated/upstream contract input changed.
