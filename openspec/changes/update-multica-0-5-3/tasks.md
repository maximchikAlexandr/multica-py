## 1. Freeze the approved Multica 0.5.3 patch contract

- [x] 1.1 Record the exact implementation base and independently reverify `v0.5.3`, release `395523214`, peeled commit `ff8b285497809e084915016c40c2bc5e5991ffbc`, official target archive, executable and version JSON SHA-256 identities, checksum manifest, unsigned-tag status, and the exact `0.5.2...0.5.3` comparison in ignored storage.
- [x] 1.2 Reproduce the `0.5.3` release-note relevance and breaking/migration/compatibility review, explicitly separating resumed Claude usage from excluded PR automation, UI, daemon, messaging, mobile, runtime, documentation, and pricing changes.
- [x] 1.3 Reproduce the complete `201 -> 201` command audit with every node retained, no added/removed/renamed/moved/help-changed node, stable source-only classifications, and reviewed positional, flag, default, alias, accepted-value, presence, conflict, destination, and source data.
- [x] 1.4 Reproduce the complete 167-entrypoint response review with `response_review_complete=true`, unchanged source-backed wire decisions, the separate usage-semantic provenance decision, and explicit retain/not-SDK dispositions.
- [x] 1.5 Update and strictly validate `contracts/sdk-contract.json` for target `0.5.3`, maximum-tested `0.5.3`, exclusive ceiling `0.5.4`, exact provenance, unchanged operation/response surface, existing gates, negative inventory, and test references.

## 2. Render and verify the narrow compatibility delta

- [x] 2.1 Render the approved contract twice to isolated destinations, require byte-identical results, commit only the deterministic generated runtime projection, and check the target, maximum-tested, ceiling, source references, and unchanged operation/field gates.
- [x] 2.2 Extend existing frozen usage fixtures for resumed-session baseline subtraction, multi-model input/output/cache values, reset, missing/corrupt snapshot, no-result fallback, rejected resume, and agent/issue/runtime aggregate payloads; assert exact target-value preservation with no SDK arithmetic.
- [x] 2.3 Extend existing contract, provenance, source-link, compatibility, operation, response, public-inventory, and negative-scope cases for the exact `0.5.2 -> 0.5.3` interval without changing public symbols, signatures, models, fields, operations, relations, or dependencies.

## 3. Publish migration and release evidence

- [x] 3.1 Update README, API, compatibility, migration, changelog, prepared-live, maintainer, and release guidance for direct `0.5.2 -> 0.5.3`, unchanged public shape, corrected resumed Claude usage, explicit exclusions, and atomic rollback.
- [x] 3.2 Run strict OpenSpec validation; approved-contract validate/render/check; deterministic comparison; source-link audit; focused contract and usage suites; Ruff check and format; `mypy src`; `mypy tests`; complete `pytest -m "not live"`; and collect-only verification proving no live node is selected.
- [x] 3.3 Build wheel and source distribution, run isolated package validation, audit tracked/package contents for forbidden evidence or transient files, report prepared-live status separately, and deliver one clean exact implementation commit.
