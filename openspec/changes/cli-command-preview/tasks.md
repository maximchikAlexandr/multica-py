## 1. Shared command-plan infrastructure

- [x] 1.1 Create `src/multica_py/_internal/commands.py` with a private frozen `_StepRef` (kind: `Literal["result", "temp"]`, field: `str | None`, alias: `str | None` for display) and a private frozen `_Step` (argv: `tuple[str, ...]`, mode: `Literal["run_bytes", "run_text", "spawn"]`, stdin: `bytes | None = None`, timeout: `datetime.timedelta | None = None`, refs: `tuple[tuple[int, _StepRef], ...] = ()`, decode: `Callable[[bytes, str], object] | None = None`, result_alias: `str | None = None`). No public exports from this module except via `Command`.
- [x] 1.2 In `_internal/commands.py`, add a private frozen `_CommandPlan[T]` (Generic) holding `config_snapshot: ClientConfig`, `transport: CliTransport`, `steps: tuple[_Step, ...]`, `finalize: Callable[[tuple[object, ...]], T]`, and an optional `_temp_provider` hook for local-I/O steps. Add a private renderer that, for each step, (a) substitutes ref positions with their display form (`${<alias>.<field>}` for result refs, `${temp.path}` for temp refs), (b) calls `transport.build_full_argv(step.argv)`, (c) calls `redact_argv(...)`, (d) `shlex.join(...)`s the result; returns `tuple[str, ...]`. Add a private runner that iterates steps, resolves refs from prior results / the temp provider, calls `transport.<mode>(step.argv, stdin=step.stdin, timeout=step.timeout)`, decodes via `step.decode`, collects results, and calls `finalize(results)`; on a `run_bytes`/`run_text` step it relies on the transport's existing nonzero-exit/exception behavior; an empty `steps` tuple returns `finalize(())` with no I/O.
- [x] 1.3 Add the public `Command[T]` class (Generic) in `_internal/commands.py` with `__init__(self, plan: _CommandPlan[T])`, `@property commands(self) -> tuple[str, ...]` (calls the plan renderer), `run(self) -> T` (calls the plan runner), and `__repr__` returning a redacted short form (e.g. `Command(commands=...)`). `Command` is generic with no public mutable state; the plan is private. Re-export `Command` from `src/multica_py/__init__.py` and add it to `__all__`.
- [x] 1.4 In `src/multica_py/_internal/transport.py`, add `def build_full_argv(self, command_args: tuple[str, ...]) -> tuple[str, ...]: return (str(self._config.executable), *build_global_args(self._config), *command_args)`. Refactor `_build_full_argv` to delegate to `build_full_argv`. `_execute`/`spawn` continue to call it internally; no other transport behavior changes.
- [x] 1.5 In `src/multica_py/resources/_base.py`, add a `BaseResource._plan(self, *, steps, finalize, temp_provider=None) -> Command[T]` helper that snapshots `self._config` via `msgspec.structs.replace(self._config)` and constructs a `_CommandPlan` bound to `self._transport`. Add a `_plan_decode(self, args, model_type)` / `_plan_decode_list(self, args, item_type)` pair returning per-step decode closures that append `--output json` and call `decode_json` (mirroring `_run_json_decode` / `_run_json_decode_list`); keep `_run_json_decode` / `_run_json_decode_list` as the execution-time decoders used by the closures. No `Any`.
- [x] 1.6 Run `uv run mypy src` and `uv run mypy tests`; fix until green. No `Any` leaks. (No tests yet — the helper is exercised once task 2 lands.)

## 2. `IssueResource` and `Issue`/`TaskRun` relation command forms

- [x] 2.1 In `src/multica_py/resources/issues.py`, for each CLI-executing `IssueResource` method (`list`, `get`, `pull_requests`, `children`, `create`, `update`, `assign`, `set_status`, `deprioritize`, `reorder`, `search`, `runs`, `run_messages`, `usage`, `rerun`, `cancel_task`), add a typed `<method>_command(...)` sibling with the same `@overload` set, the same `_resolve_request` validation (for `create`/`update`/`assign`/`reorder`), and the same `validate_nonblank`/`ValueError` checks, raised before any plan step runs. Each `<method>_command` builds a `_CommandPlan` via `BaseResource._plan` with the method's argv and execution mode (`run_bytes` for JSON-decoding methods, `run_text` for `deprioritize`/`rerun`/`cancel_task`, `spawn` is not used by `IssueResource`). The eager method body becomes `return self.<method>_command(...).run()`. For `create` with `label_ids`, build a composite plan: step 1 `issue create ... --output json` (alias `create`), one `issue label add ${create.id} <label_id>` step per label (ref at the id argv position), final `issue get ${create.id} --output json` step; the plan's `finalize` returns the final `Issue` bound with `self._client`.
- [x] 2.2 In `src/multica_py/resources/issues.py`, add command forms for the `Issue` lazy-relation entry points by delegating to the lazy object's `all_command()`/`refresh_command()`/`page_command()` (added in task 11) where the lazy object owns the loader. For the `Issue` mutation methods (`add_comment`, `reply`, `add_label`, `remove_label`, `add_subscriber`, `remove_subscriber`, `set_metadata`, `delete_metadata`), add `<method>_command(...)` siblings that build a single-step plan (or a two-step plan where the mutation invalidates a cache: the mutation step + a reload step if the eager method currently reloads). The eager mutation method becomes `return self.<method>_command(...).run()`. Preserve the existing cache invalidation behavior inside the plan's `finalize` (or after the step runs).
- [x] 2.3 In `src/multica_py/resources/issues.py`, add `TaskRun.messages_command()` returning `Command[tuple[RunMessage, ...]]` whose plan delegates to `client.issues.run_messages_command(task_run_id, issue_id=issue_id)`. The eager `messages` property stays a property that calls `self.messages.all()` (unchanged). Verify `TaskRun` relation command forms route through the `IssueResource.run_messages_command` plan.
- [x] 2.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 3. `ProjectResource` and `Project` relation command forms

- [x] 3.1 In `src/multica_py/resources/projects.py`, add `<method>_command(...)` for `list`, `get`, `create`, `update`, `delete`, `set_status` (all `run_bytes`/`run_text` per current behavior; `delete` and `set_status` use `run_text`/`run_bytes` exactly as today). Eager bodies become `return self.<method>_command(...).run()`. Preserve the dual-input `@overload` set for `create`/`update`.
- [x] 3.2 Add `Project.add_local_directory_command(...)` and `Project.remove_resource_command(...)` siblings; eager methods delegate. The plan for `add_local_directory` includes the `os.path.abspath` normalization in the step argv (the path is resolved at plan-build time, before `run()`), and the existing `_invalidate_resources` cache invalidation runs in `finalize` after the step succeeds.
- [x] 3.3 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 4. `AgentResource` and `Agent` relation command forms

- [x] 4.1 In `src/multica_py/resources/agents.py`, add `<method>_command(...)` for `list`, `get`, `create`, `update`, `archive`, `restore`, `tasks`, `avatar`. `avatar` uses `run_text` (or the current transport method); `archive`/`restore` use `run_text`. Eager bodies delegate. Preserve the dual-input `@overload` set for `create`/`update`.
- [x] 4.2 Add `Agent.set_skills_command(...)`; eager `set_skills` delegates and preserves the `_invalidate_skills` cache invalidation in `finalize`.
- [x] 4.3 Add `Agent.skills.all_command()`/`refresh_command()` delegation (via task 11) and `Agent.tasks.all_command()`/`refresh_command()` and `Agent.issues.all_command()`/`refresh_command()`/`page_command()`.
- [x] 4.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 5. `SkillResource` and `Skill` relation command forms

- [x] 5.1 In `src/multica_py/resources/skills.py`, add `<method>_command(...)` for `list`, `get`, `create`, `update`, `delete`, `import_from_url`. Eager bodies delegate. Preserve dual-input `@overload` for `create`/`update`.
- [x] 5.2 Add `Skill.upsert_file_command(...)` and `Skill.delete_file_command(...)`; eager methods delegate and preserve `_invalidate_files` in `finalize`.
- [x] 5.3 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 6. `AutopilotResource` and `Autopilot`/`AutopilotRun` relation command forms

- [x] 6.1 In `src/multica_py/resources/autopilots.py`, add `<method>_command(...)` for `list`, `get`, `create`, `update`, `delete`, `trigger`, `history`, `trigger_add`, `trigger_update`, `trigger_delete`. Eager bodies delegate. Preserve the dual-mode/overload behavior of `create`/`update` (presence-aware `Unset` fields).
- [x] 6.2 Add `Autopilot.runs.all_command()`/`refresh_command()`/`page_command()`, `Autopilot.triggers.all_command()`/`refresh_command()`, `Autopilot.subscribers.all_command()`/`refresh_command()`, `Autopilot.trigger_add_command()`/`trigger_update_command()`/`trigger_delete_command()` (with cache invalidation in `finalize`).
- [x] 6.3 Add `AutopilotRun.messages_command()`; eager `messages` property stays a property that calls `self.messages.all()`. The plan raises the typed relation-context error (missing `task_id`) at plan-build time, before any I/O.
- [x] 6.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 7. `SquadResource` and `Squad` relation command forms

- [x] 7.1 In `src/multica_py/resources/squads.py`, add `<method>_command(...)` for `list`, `get`. Eager bodies delegate.
- [x] 7.2 Add `Squad.add_member_command(...)` / `Squad.remove_member_command(...)` with `_invalidate_members` in `finalize`; eager methods delegate. Add `Squad.members.all_command()`/`refresh_command()` and `Squad.issues.all_command()`/`refresh_command()`/`page_command()`.
- [x] 7.3 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 8. `WorkspaceResource` and `Workspace`/`WorkspaceMember` relation command forms

- [x] 8.1 In `src/multica_py/resources/workspaces.py`, add `<method>_command(...)` for `list`, `get`, `members`, `switch`, `watch`, `unwatch`. Eager bodies delegate.
- [x] 8.2 Add `Workspace` relation command forms: `members`/`agents`/`skills`/`projects`/`labels`/`repositories`/`runtimes`/`squads` `all_command()`/`refresh_command()`; `issues.all_command()`/`refresh_command()`/`page_command()`; `autopilots.all_command()`/`refresh_command()` (with `metadata.total` preserved in `finalize`).
- [x] 8.3 Add `WorkspaceMember.issues.all_command()`/`refresh_command()`/`page_command()`.
- [x] 8.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 9. Nested resource command forms

- [x] 9.1 In `src/multica_py/resources/issue_comments.py`, add `<method>_command(...)` for `list_flat`, `list_recent`, `list_thread`, `add`, `reply`, `resolve` (and any other CLI-executing public methods). Eager bodies delegate. Preserve cursor pagination semantics for `list_recent`/`list_thread`.
- [x] 9.2 In `src/multica_py/resources/issue_labels.py`, add `<method>_command(...)` for `list`, `add`, `remove`. Eager bodies delegate.
- [x] 9.3 In `src/multica_py/resources/issue_metadata.py`, add `<method>_command(...)` for `list`, `set`, `set_typed`, `delete`, `query` (and the other public methods). Eager bodies delegate. Preserve the dual-input overload for `set_typed` if present.
- [x] 9.4 In `src/multica_py/resources/issue_subscribers.py`, add `<method>_command(...)` for `list`, `add`, `remove`. Eager bodies delegate.
- [x] 9.5 In `src/multica_py/resources/agent_skills.py`, add `<method>_command(...)` for `list`, `set`. Eager bodies delegate.
- [x] 9.6 In `src/multica_py/resources/skill_files.py`, add `<method>_command(...)` for `list`, `create`, `update`, `delete` (and any other public CLI-executing methods). Eager bodies delegate.
- [x] 9.7 In `src/multica_py/resources/project_resources.py`, add `<method>_command(...)` for `list`, `add_local_directory`, `update_local_directory`, `remove`. Eager bodies delegate. Preserve dual-input `@overload` for `add_local_directory`/`update_local_directory`.
- [x] 9.8 In `src/multica_py/resources/squad_members.py`, add `<method>_command(...)` for `list`, `add`, `remove` (and `set_role` if CLI-executing). Eager bodies delegate.
- [x] 9.9 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 10. Remaining top-level resources

- [x] 10.1 In `src/multica_py/resources/daemon.py`, add `<method>_command(...)` for `start` (spawn), `status`, `stop`, `restart`, `disk_usage`, `logs` (spawn, with the `--follow` flag). Eager bodies delegate. The `start_command`/`logs_command` plans retain `spawn` execution; `run()` calls `transport.spawn` and returns `ManagedProcess`.
- [x] 10.2 In `src/multica_py/resources/auth.py`, add `status_command`, `login_command(token: str | None = None)` (dual-mode: `token is not None` → `run_text` with `--token <redacted>` → `Command[str]`; `token is None` → `spawn` → `Command[ManagedProcess]`), `logout_command`. Preserve the `@overload` set of `login`. Eager bodies delegate.
- [x] 10.3 In `src/multica_py/resources/attachments.py`, add `upload_command(path, *, task_id=None)`, `upload_bytes_command(filename, payload, *, task_id=None)` (single-step plan with a `${temp.path}` ref and a lazy `_TempPathProvider` that creates a `TemporaryDirectory` on first resolve and cleans up in a `finally` around `run()`), `download_command(attachment_id, *, output_dir)`, `download_bytes_command(attachment_id)` (single-step plan with a `${temp.path}` ref for the output dir; `finalize` reads the downloaded bytes via the existing `_read_downloaded_bytes` and cleans up the temp dir in `finally`). Preserve `_safe_leaf` validation at plan-build time. Eager bodies delegate.
- [x] 10.4 In `src/multica_py/resources/runtimes.py`, add `<method>_command(...)` for `list`, `usage`, `activity`, `update`, `rename`, `delete`. Eager bodies delegate. Preserve dual-input `@overload` for `update`.
- [x] 10.5 In `src/multica_py/resources/repositories.py`, add `<method>_command(...)` for `list`, `add`, `remove`. Eager bodies delegate.
- [x] 10.6 In `src/multica_py/resources/labels.py`, add `<method>_command(...)` for `list`. Eager body delegates.
- [x] 10.7 In `src/multica_py/resources/users.py`, add `<method>_command(...)` for `profile_get`, `profile_update`. Eager bodies delegate. Preserve dual-input `@overload` for `profile_update`.
- [x] 10.8 In `src/multica_py/resources/setup.py`, add `<method>_command(...)` for `cloud`, `self_host` (both spawn). Eager bodies delegate.
- [x] 10.9 In `src/multica_py/resources/maintenance.py`, add `<method>_command(...)` for `update` (spawn) and any other CLI-executing public methods. Eager bodies delegate.
- [x] 10.10 In `src/multica_py/resources/configuration.py`, add `<method>_command(...)` for every CLI-executing public method. Eager bodies delegate.
- [x] 10.11 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 11. Lazy collection/mapping command forms

- [x] 11.1 In `src/multica_py/models/relations.py`, add `LazyCollection.all_command() -> Command[tuple[T, ...]]` and `LazyCollection.refresh_command() -> Command[tuple[T, ...]]`. `all_command()` builds a plan whose `finalize` calls `self.all()`; for a loaded relation the plan has `steps == ()` (so `commands == ()`) and `finalize` returns the cached value with no I/O. `refresh_command()` always builds a loader plan whose `finalize` calls `self.refresh()` (force=True), preserving the existing generation/coalescing/retry semantics. The plan delegates argv construction to the resource's `*_command()` by calling the loader closure inside `finalize`; the lazy object does not build argv itself.
- [x] 11.2 Add `OffsetLazyCollection.page_command(*, limit: int | None = None, offset: int = 0) -> Command[OffsetPage[T]]` building an exact, static single-page plan from the page loader's argv template. Add `OffsetLazyCollection.all_command()` as a composite plan that creates each runtime page from the same `page_command` factory; before execution, `commands` shows the exact first page and one explicit next-page template with a result reference (e.g. `${page.next_offset}`); after each page is resolved, the concrete argv sent to the transport is derived from that same template. Preserve the existing repeated-offset/empty-page/maximum-page/maximum-item guards.
- [x] 11.3 Add `CursorLazyCollection.page_command(*, cursor: CommentCursor | None = None) -> Command[CursorPage[T]]` and `CursorLazyCollection.all_command()` (composite plan with a `${page.next_cursor}` next-page template). Preserve the existing repeated-cursor/empty-page/maximum-page/maximum-item guards.
- [x] 11.4 Add `LazyMapping.all_command() -> Command[Mapping[K, V]]` and `LazyMapping.refresh_command() -> Command[Mapping[K, V]]` with the same cache-hit / refresh semantics as `LazyCollection`.
- [x] 11.5 Verify `MulticaClient.prefetch` (`src/multica_py/client.py`) loads each selected relation through `relation.all()` (now `all_command().run()`); no `prefetch_command()` is added. Preserve deduplication, origin-scope validation, parallelism bounds, and fail-fast first-failure behavior.
- [x] 11.6 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 12. Test migration and focused command-preview coverage

- [x] 12.1 In `tests/cases/operations.py`, extend `OperationCase` with `expected_commands: tuple[str, ...] = ()` (the full rendered `commands` tuple including executable + global args + `shlex.join` quoting) and, if needed to avoid duplication, a `command_preview: tuple[str, ...] = ()` alias. Populate `expected_commands` for every canonical `OperationCase` from the same plan that produces `expected_argv`. Do not create a parallel `CommandPreviewCase` type.
- [x] 12.2 In `tests/unit/resources/test_operations.py`, rewrite `_assert_transport_call` to: (a) construct `command = getattr(resource, f"{case.method}_command")(*case.args, **dict(case.kwargs))`; (b) assert `command.commands == case.expected_commands`; (c) call `result = command.run()`; (d) `case.assert_result(result, mock_transport)`; (e) assert the transport received argv/execution-mode/stdin/timeout derived from the same plan, normalizing only `case.dynamic_argv_positions`. Keep `test_discovered_public_methods` green.
- [x] 12.3 Strengthen `test_discovered_public_methods`: after the existing discovered == canonical assertion, assert that every canonical CLI-executing `sdk_method` has a `<method>_command` attribute on its resource class (and, for relation entry points, on the lazy object class) and that `OPERATION_CASES` has a command-preview case (`expected_commands` populated) for it. Exclude local-only methods (e.g. `invalidate()`). The strengthened gate fails closed when a new public CLI-executing method is added without a command form and command-preview case.
- [x] 12.4 In `tests/unit/resources/test_operations.py` (or a new focused `tests/unit/resources/test_command_preview.py`), add focused cases (as `OperationCase` rows or a small frozen `@dataclass(frozen=True)` `CommandFocusCase` table) covering: no-I/O command construction (assert zero transport calls at construction); one-command, multi-command (`issues.create` with `label_ids`), `run_text` (`issues.deprioritize`), `run_bytes` (`issues.get`), and `spawn` (`daemon.start`) plans; global args and `shlex.join` shell quoting (argument with a space/quote); token redaction in `commands`/`repr` without changing executed argv (`auth.login` with token); stdin and timeout preservation; runtime path resolution (`attachments.upload_bytes`/`download_bytes` with `${temp.path}`); result-reference resolution (`issues.create` `${create.id}`); cache hit (`relation.all_command()` after load → `commands == ()`) and forced refresh (`relation.refresh_command()`); offset/cursor `page_command()` and the no-progress/empty-page failure guards; prefetch calling relation `all_command().run()` under concurrency; command/config snapshot behavior (construct under `with_profile("a")`, change to `"b"`, assert `commands` and executed argv still reflect `"a"`); composite failure stops at the correct step (first-step failure of `issues.create` with labels → typed exception, no later step executed, no rollback). Reuse shared fixtures (`mock_transport`, `make_target`, `make_settings`); do not duplicate local helpers.
- [x] 12.5 Migrate `tests/component/test_public_operation_routing.py`, `tests/component/resources/cases.py`, the relation loader cases (`tests/unit/resources/test_*_relations.py`), the process/spawn cases (`tests/unit/test_process_*.py`, `tests/component/test_process_contract.py`), and the redaction cases to assert CLI routing through the public `*_command()` path wherever they assert CLI routing. Transport-only tests (`tests/unit/test_transport.py`, the three real-process cases `bytes-env`/`text-stdin`/`timeout-tree-cleanup`) keep testing `CliTransport` directly.
- [x] 12.6 Migrate `tests/live/test_smoke.py` to use the public `*_command()` path wherever it asserts CLI routing; keep the triple marker (`pytest.mark.live`, `pytest.mark.live_smoke`, `pytest.mark.serial`) on the module. Verify with `uv run pytest -m "not live" --collect-only` that no `tests/live/*` node is collected.
- [x] 12.7 Run `uv run pytest -m "not live"`, `uv run mypy src`, `uv run mypy tests`, `uv run ruff check`, `uv run ruff format --check`; fix until green.

## 13. Public exports and docs

- [x] 13.1 In `src/multica_py/__init__.py`, export `Command` (from `multica_py._internal.commands` or a new `multica_py.commands`) and add it to `__all__`. No other public type is added; no existing public type is removed or renamed.
- [x] 13.2 In `docs/api.md`, document `Command[T]` (`commands`, `run()`) and the `*_command()` convention; state that `commands` is always a tuple (empty for a no-op, one item for one CLI call, ordered items/templates for a composite operation), that preview performs no I/O, and that `run()` executes the same immutable plan.
- [x] 13.3 In `docs/service-usage.md`, add a section showing `client.issues.get_command("issue_123")` and `command.commands` / `command.run()` as the inspectable alternative to the eager form; label the eager form as the default. Add a composite example (`issues.create_command` with `label_ids`) showing the ordered plan and `${create.id}`.
- [x] 13.4 In `docs/migration.md`, add a note that eager APIs and return types are unchanged, that `Command[T]` is the only new public type, and that `commands` is a tuple with the documented empty/one/ordered cases. No breaking change is recorded (this is an additive release).
- [x] 13.5 Update `tests/contract/test_bound_public_surface.py` to include `Command` in the public export table; update `tests/contract/test_bound_public_docs.py` to pin the new `docs/api.md`/`docs/service-usage.md` `Command` doc strings.

## 14. Final verification

- [x] 14.1 Run `uv run pytest -m "not live"` (full offline suite) end-to-end and confirm green; verify with `uv run pytest -m "not live" --collect-only` that no `tests/live/*` node is collected.
- [x] 14.2 Run `uv run mypy src` and `uv run mypy tests` end-to-end; confirm no `Any` leaks and no new errors.
- [x] 14.3 Run `uv run ruff check` and `uv run ruff format --check`; fix until green.
- [x] 14.4 Run `openspec validate cli-command-preview` and `openspec validate --specs`; fix any reported drift.
- [x] 14.5 Re-run `tests/unit/resources/test_operations.py::test_discovered_public_methods` and confirm the strengthened gate stays green (discovered == canonical, every CLI-executing canonical method has a `*_command` and a command-preview case, local-only methods excluded). Confirm `len({c.id for c in OPERATION_CASES}) == len(OPERATION_CASES)` (no case duplication).
- [x] 14.6 Confirm `contracts/sdk-contract.json` is unchanged and `tests/contract/test_sdk_contract.py` stays green (the approved contract remains the generator input; no public command support was created from evidence/heuristics).
