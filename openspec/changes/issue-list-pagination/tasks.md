## 1. Models

- [x] 1.1 In `src/multica_py/models/issues.py`, widen `IssueSummary` (line 11) by adding after `priority: str | None = None` (line 15): `created_at: datetime.datetime | None = None`, `parent_id: str | None = None`, `project_id: str | None = None`, `creator_id: str | None = None`, `creator_type: str | None = None`. All additive `None` defaults; `datetime` is already imported (line 3).
- [x] 1.2 In `src/multica_py/models/issues.py`, add `offset: int | None = None` and `project_id: str | None = None` to `IssueListFilter` (line 59) after `limit: int | None = None` (line 63) and before `sort` (line 64). No `__post_init__` on the filter (it is a plain filter struct); the nonnegative `offset` guard is added in task 3.1, not on the model.
- [x] 1.3 In `src/multica_py/models/issues.py`, add a new frozen `msgspec.Struct` `IssueListPage` after `IssueListFilter` (after line 66): fields `issues: tuple[IssueSummary, ...] = ()`, `has_more: bool = False`, `limit: int | None = None`, `offset: int | None = None`, `total: int | None = None`. `kw_only=True`, `frozen=True`.

## 2. Wire

- [x] 2.1 In `src/multica_py/_internal/wire_models.py`, widen `IssueSummaryWire` (line 26) by adding after `priority: str | None = None` (line 30): `created_at: datetime.datetime | None = None`, `parent_issue_id: str | None = None`, `project_id: str | None = None`, `creator_id: str | None = None`, `creator_type: str | None = None`. Wire field is `parent_issue_id` (upstream JSON name); the public rename to `parent_id` happens in the decoder.
- [x] 2.2 In `src/multica_py/_internal/wire_models.py`, extend `issue_summary_from_wire` (line 33) to transfer all five new fields: `created_at=wire.created_at`, `parent_id=wire.parent_issue_id` (rename), `project_id=wire.project_id`, `creator_id=wire.creator_id`, `creator_type=wire.creator_type`. Add `IssueSummary` to the import from `multica_py.models.issues` (line 17) if not already present (it is already imported at line 17).
- [x] 2.3 In `src/multica_py/_internal/wire_models.py`, widen `IssueListPageWire` (line 42) by adding after `issues: tuple[IssueSummaryWire, ...] = ()` (line 43): `has_more: bool = False`, `limit: int | None = None`, `offset: int | None = None`, `total: int | None = None`. Defaults make older `{"issues":[...]}` responses decode.
- [x] 2.4 In `src/multica_py/_internal/wire_models.py`, add a new function `issue_list_page_from_wire(wire: IssueListPageWire) -> IssueListPage` after `issue_summary_from_wire` (after line 39). Body: `return IssueListPage(issues=tuple(issue_summary_from_wire(item) for item in wire.issues), has_more=wire.has_more, limit=wire.limit, offset=wire.offset, total=wire.total)`. Add `IssueListPage` to the import from `multica_py.models.issues` (line 12-19).

## 3. Resource

- [x] 3.1 In `src/multica_py/resources/issues.py`, change `IssueResource.list` (line 50) return type annotation from `tuple[IssueSummary, ...]` to `IssueListPage`. Inside the `if filter is not None:` block (lines 53-69), after the existing `--limit` block (lines 64-65) and before the `--sort` block (lines 66-67), add: `if filter.offset is not None: args.extend(["--offset", str(filter.offset)])` and `if filter.project_id is not None: args.extend(["--project", filter.project_id])`. Add a nonnegative guard at the top of the `if filter is not None:` block, before the existing `direction_requires_sort` guard (line 54): `if filter.offset is not None and filter.offset < 0: raise ValueError("IssueResource.list: offset must be nonnegative (offset_nonnegative)")`.
- [x] 3.2 In `src/multica_py/resources/issues.py`, replace the return expression (lines 70-73) with: `return issue_list_page_from_wire(self._run_json_decode(tuple(args), IssueListPageWire))`. Update imports (lines 12-17): add `IssueListPageWire` (already imported at line 13), replace `issue_summary_from_wire` with `issue_list_page_from_wire` in the `wire_models` import; replace `IssueSummary` in the `models.issues` import (line 30) with `IssueListPage`. `IssueSummary` is no longer referenced in this file after the change (the page decoder uses it internally); remove it from the import. Leave `IssueResource.search` (line 166) returning `tuple[IssueSummary, ...]` unchanged — it is ungoverned and unaffected.

## 4. Approved contract

- [x] 4.1 In `contracts/sdk-contract.json`, add `"issue_list_page": "IssueListPage"` to the `types` catalog after `"issue_summaries": "tuple[IssueSummary, ...]"` (line 21).
- [x] 4.2 In `contracts/sdk-contract.json`, add two mappings to the `issue_list` binding descriptor's `mappings` array (lines 329-360): after the `["filter.limit", "--limit", "query:limit"]` entry (lines 345-349) insert `["filter.offset", "--offset", "query:offset"]` and after it `["filter.project_id", "--project", "query:project_id"]`. Add `"offset_nonnegative"` to the `constraints` array (lines 361-364), bringing it to `["direction_requires_sort", "offset_nonnegative", "position_forbids_direction"]`.
- [x] 4.3 In `contracts/sdk-contract.json`, add two `"optional_omit"` entries to `mapping_presence.issue_list` (lines 734-741): append two more `"optional_omit"` so the array has eight entries (six existing + offset + project_id), matching the eight mappings.
- [x] 4.4 In `contracts/sdk-contract.json`, add the same two mapping objects to the `issues.list` operation entry's `mappings` array (lines 1517-1548), in the same position (after `filter.limit`/`--limit`/`query:limit`, before `filter.sort`). Add `"offset_nonnegative"` to `validator_ids` (lines 1549-1552), bringing it to `["direction_requires_sort", "offset_nonnegative", "position_forbids_direction"]`.
- [x] 4.5 In `contracts/sdk-contract.json`, update the `issue_list` signature (line 44) to `"(filter: IssueListFilter | None = None) -> IssueListPage"`.
- [x] 4.6 In `contracts/sdk-contract.json`, add a new response block `issue_list_page` after the `issue_summaries` block (after line 833): `{"public_type_id": "issue_list_page", "wire_type_id": "issue_list_page_wire", "decoder_id": "decode_issue_list_page", "success_exit_codes": [0], "malformed_output": "raise_output_shape_or_decode_error"}`. Leave the existing `issue_summaries` block (lines 825-833) in place (still referenced by the catalog; `IssueResource.search` is ungoverned and uses `IssueSummary` directly).
- [x] 4.7 In `contracts/sdk-contract.json`, register the decoder in the `decoders` map (after line 886): `"decode_issue_list_page": "multica_py._internal.wire_models.issue_list_page_from_wire"`.
- [x] 4.8 In `contracts/sdk-contract.json`, repoint the `issues.list` entrypoint `response_id` from `"issue_summaries"` to `"issue_list_page"` (line 3700).
- [x] 4.9 In `contracts/sdk-contract.json`, flip the `issues.list` operation `compatibility` from `"compatible"` to `"intentionally_changed"` (line 3686) and set `rationale` to `"Return type widens from tuple[IssueSummary, ...] to IssueListPage, exposing pagination metadata; offset and project_id filters added."`.
- [x] 4.10 In `tools/upstream_contract/contract.py`, add `"issue_list_page"` to the `_AUXILIARY_CATALOG_KEYS` `types` frozenset (after line 94, near `"issue_summaries"` at line 95) and `"decode_issue_list_page"` to the `decoders` frozenset (after line 138, near `"decode_issue_summaries"`). These keep the contract validator from rejecting the new catalog keys.
- [x] 4.11 In `contracts/sdk-contract.json`, update the `generated:issues.list:default:canonical` test vector (lines 2247-2274): change `stdout_base64` from `"eyJpc3N1ZXMiOltdfQ=="` to the base64 of `{"issues":[],"has_more":false,"limit":50,"offset":0,"total":0}` (compute with `python3 -c "import base64,json; print(base64.b64encode(json.dumps({'issues':[],'has_more':False,'limit':50,'offset':0,'total':0}).encode()).decode())"`) and change `assertion.expected.value` from `"builtins.tuple"` to `"multica_py.models.issues.IssueListPage"`.

## 5. Generator output

- [x] 5.1 Run `uv run python scripts/upstream_contract.py validate --approved contracts/sdk-contract.json` (without `--source-checkout`: no new `source_ref` is added; the existing `S-ISSUE` already covers `cmd_issue.go` including `runIssueList`). Must pass.
- [x] 5.2 Run `uv run python scripts/upstream_contract.py render --approved contracts/sdk-contract.json --runtime-output src/multica_py/_generated/approved_sdk.py --transient-output /tmp/multica-transient`. The transient path MUST be outside tracked dirs per AGENTS.md.
- [x] 5.3 Re-run the same render command and confirm `git diff --stat src/multica_py/_generated/approved_sdk.py` is empty (idempotent). The `ISSUE_LIST_BINDING` (src/multica_py/_generated/approved_sdk.py:88) gains the two new mappings. Remove `/tmp/multica-transient` after.

## 6. Tests

- [x] 6.1 In `tests/cases/operations.py`, update the canonical `generated:issues.list:default:canonical` row (lines 1080-1085): set `stdout` to `b'{"issues":[],"has_more":false,"limit":50,"offset":0,"total":0}'` and add `assert_result` — a callable asserting `type(result).__module__ + "." + type(result).__qualname__ == "multica_py.models.issues.IssueListPage"` (mirror the inline `assert_type` pattern in `generated_operation_cases.assertion_for`, lines 230-235). Because this row is generated from the contract vector, instead update the contract vector per task 4.11 and let `generated_operation_cases` materialize the assertion from `assertion.kind == "decoded_type"` → `assert_type` automatically; no manual `assert_result` is needed on the generated row. Confirm by reading `assertion_for` (lines 224-235): a `decoded_type` assertion with `expected.value == "multica_py.models.issues.IssueListPage"` produces the right assert callable.
- [x] 6.2 In `tests/cases/operations.py`, add three `manual:` variant rows for `issues.list` after the canonical row (after line 1085), using `_c(...)`:
  - `manual:issues.list:variant:01` — `args=(IssueListFilter(offset=20),)`, `expected_argv=("issue","list","--offset","20","--output","json")`, `stdout=b'{"issues":[],"has_more":false,"limit":50,"offset":20,"total":0}'`. Import `IssueListFilter` in the `_build_operation_cases` local imports (add to the `from multica_py.models.issues import (...)` block, lines 455-465).
  - `manual:issues.list:variant:02` — `args=(IssueListFilter(project_id="pr_001"),)`, `expected_argv=("issue","list","--project","pr_001","--output","json")`, `stdout=b'{"issues":[],"has_more":false,"limit":50,"offset":0,"total":0}'`.
  - `manual:issues.list:variant:03` — `args=(IssueListFilter(offset=20, project_id="pr_001"),)`, `expected_argv=("issue","list","--offset","20","--project","pr_001","--output","json")`, `stdout=b'{"issues":[],"has_more":false,"limit":50,"offset":20,"total":0}'`.
  No `assert_result` needed (transport-call assertion only). These are noncanonical (`:variant:`), so the canonical method set is unchanged.
- [x] 6.3 In `tests/cases/operations.py`, add three entries to `LEGACY_ARGV_MIGRATION` (after line 429): `"legacy:144": "manual:issues.list:variant:01"`, `"legacy:145": "manual:issues.list:variant:02"`, `"legacy:146": "manual:issues.list:variant:03"`.
- [x] 6.4 In `tests/cases/legacy_payloads.py`, append three fingerprints for `legacy:144/145/146` computed with the existing helper formula `hashlib.sha256(repr(payload(case)).encode()).hexdigest()` where `payload` is the tuple from `tests/unit/resources/test_operations.py::test_legacy_payload_bijection` (lines 103-114). The fingerprint list grows 143 → 146.
- [x] 6.5 In `tests/unit/resources/test_operations.py::test_discovered_public_methods` (lines 79-95), update the counter assertions: `len(discovered) == 117` (unchanged), `len(OPERATION_CASES) == 149` (was 141), canonical count `== 117` (unchanged), noncanonical `== 32` (was 24: 24 existing `:variant:` rows + the 8 new variants added by the decode-executor-fields-and-squad-members change), `len(generated) == 37` (was 30), `len(manual) == 112` (was 111). Update `test_legacy_payload_bijection` (lines 116-119): `range(1, 139)` → `range(1, 147)` and `len(LEGACY_PAYLOAD_FINGERPRINTS) == 146` (was 138) and the bijection length `== 146` (was 138). Recompute every counter against the actual edited `OPERATION_CASES` before committing — these are exact invariants, not estimates.
- [x] 6.6 In `tests/contract/test_issue_models.py`, add imports: `IssueListFilter` and `IssueListPage` from `multica_py.models.issues`, and `IssueListPageWire, issue_list_page_from_wire` from `multica_py._internal.wire_models` (extend the existing import at line 9).
- [x] 6.7 In `tests/contract/test_issue_models.py`, add `test_issue_list_page_decoding`: decode `{"issues":[{"id":"i1","title":"t","status":"todo","created_at":"2026-01-01T00:00:00Z","parent_issue_id":"p1","project_id":"pr1","creator_id":"u1","creator_type":"member"}],"has_more":true,"limit":50,"offset":20,"total":137}` via `decode_json(..., IssueListPageWire)` then `issue_list_page_from_wire`; assert `page.has_more is True`, `page.limit == 50`, `page.offset == 20`, `page.total == 137`, `len(page.issues) == 1`, `page.issues[0].created_at == datetime.datetime(2026,1,1,tzinfo=datetime.timezone.utc)`, `page.issues[0].parent_id == "p1"`, `page.issues[0].project_id == "pr1"`, `page.issues[0].creator_id == "u1"`, `page.issues[0].creator_type == "member"`. Add an omitted-metadata assertion: decode `{"issues":[]}` → `page.has_more is False`, `page.limit is None`, `page.offset is None`, `page.total is None`, `page.issues == ()`.
- [x] 6.8 In `tests/contract/test_issue_models.py`, add `test_issue_summary_scalar_fields_decoding`: decode `{"id":"i1","title":"t","status":"todo"}` via `decode_json(..., IssueSummary)` → `created_at is None`, `parent_id is None`, `project_id is None`, `creator_id is None`, `creator_type is None`; decode `{"id":"i1","title":"t","status":"todo","created_at":"2026-01-01T00:00:00Z","parent_issue_id":"p1","project_id":"pr1","creator_id":"u1","creator_type":"member"}` via `decode_json(..., IssueSummaryWire)` then `issue_summary_from_wire` → all five fields present and `parent_id == "p1"` (rename). Add `IssueSummaryWire, issue_summary_from_wire` to the imports if not present.
- [x] 6.9 In `tests/contract/test_issue_models.py`, add `@pytest.mark.parametrize` `test_issue_list_filter_rejects_negative_offset` over rows `(-1,)` and `(-5,)`: construct `IssueListFilter(offset=value)` and assert `ValueError` is raised and `"offset"` appears in the message via a direct call into `IssueResource.list` with a fake transport (the guard is in `IssueResource.list`, not the filter model). Use the `mock_transport` fixture and `ClientConfig()` like `tests/unit/resources/test_operations.py::_assert_transport_call` (lines 45-55): build `IssueResource(mock_transport, ClientConfig())`, call `.list(IssueListFilter(offset=-1))`, assert `ValueError` and `"offset"` in the message, and assert `mock_transport.run_bytes.assert_not_called()`. Add one row asserting `offset=0` is valid: `.list(IssueListFilter(offset=0))` does not raise (it emits `--offset 0`); assert `mock_transport.run_bytes` was called once with argv ending `("issue","list","--offset","0","--output","json")`.

## 7. Verification

- [x] 7.1 `uv run pytest -m "not live"` green.
- [x] 7.2 `uv run mypy src` and `uv run mypy tests` green.
- [x] 7.3 `uv run ruff check` and `uv run ruff format --check` green.
- [x] 7.4 `uv run pytest tests/unit/resources/test_operations.py::test_discovered_public_methods` asserts the canonical method set is unchanged (117 methods) and the updated counters (149 total / 117 canonical / 32 noncanonical / 37 generated / 112 manual) — no allowlist, exact invariants.
- [x] 7.5 `uv run pytest tests/unit/resources/test_operations.py::test_legacy_payload_bijection` green with 146 legacy fingerprints.
- [x] 7.6 `uv run openspec change validate issue-list-pagination --strict` green.