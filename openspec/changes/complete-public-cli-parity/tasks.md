## 1. Freeze the complete public CLI inventory

- [ ] 1.1 Record the implementation base, pinned source/release/archive/executable/version identities, repository dirty state, and reproducible evidence commands without tracking downloaded evidence.
- [ ] 1.2 Extend source extraction to resolve production command variables and `AddCommand` topology while excluding test-only registrations and retaining unknown patterns as review items.
- [ ] 1.3 Reconcile recursive `v0.5.3` binary help with pinned source into one public command tree, including positional arity, aliases, hidden state, inherited flags, and defaults.
- [ ] 1.4 Resolve dynamic issue-wakeup factories, daemon flag aliases, imperative `Flags().Changed` branches, validators, and value destinations; leave no relevant unresolved source pattern.
- [ ] 1.5 Trace every public leaf through `RunE` and helpers to path, query, JSON, header, multipart, stdin, file, or local-process behavior and record atomic/lifecycle semantics.
- [ ] 1.6 Trace every public output path, variant, nested field, acknowledgement, pagination signal, stderr cursor, and error/exit behavior to concrete source types and writes.
- [ ] 1.7 Assign exactly one evidence-backed parity disposition to every command, input, transport, response, and field item and prove every equivalent, presentation-only, and exclusion decision.
- [ ] 1.8 Add a review report that explicitly reconciles all A1–A10, B1–B17, C1–C24, D1–D17, false-positive warnings, and any additional discovered gaps.

## 2. Make the approved contract enforce parity

- [ ] 2.1 Extend the approved contract schema with stable command/input/output/field inventory identities, allowed dispositions, evidence links, public symbol or transport links, compatibility, and test references.
- [ ] 2.2 Add strict validation for unique and complete public inventory coverage, effective inherited flags, resolved factories/aliases, and rejection of public partial/unknown/deferred/unreviewed rows.
- [ ] 2.3 Add semantic validation for typed-equivalent, transport, presentation-only, and outside-public-scope rows, including required rationale and source/test evidence.
- [ ] 2.4 Promote reviewed chat, wakeup, runtime-profile, repository-checkout, and other missing operations into approved operation/source/response/test catalogs; remove superseded public deferrals.
- [ ] 2.5 Add every missing input mapping with five-state presence, destination, constraints, atomicity, secret policy, and compatibility; retain presentation spellings only in inventory.
- [ ] 2.6 Replace response-subset review with complete CLI-output review entries and reviewed wire/public shape mappings for every response-producing operation.
- [ ] 2.7 Update deterministic rendering and the committed generated runtime projection to consume only reviewed contract additions and remain byte-stable across two renders.
- [ ] 2.8 Add contract tests proving extractor evidence cannot promote behavior, unclassified public drift fails closed, all public symbols resolve, and every CLI operation has exactly one canonical vector.

## 3. Correct broken argv, transport, and decoder contracts

- [ ] 3.1 Correct daemon status and disk-usage variants/models for actual status strings, duration strings, report objects, aggregates, and absence semantics; add public native-shape regressions for A1–A2.
- [ ] 3.2 Correct daemon stop/restart and auth status/logout so unsupported output flags and fabricated JSON results are removed and lifecycle/text behavior is represented safely for A3–A4.
- [ ] 3.3 Generalize project-resource wire/public variants and collection conversion for every CLI-supported resource reference without weakening local-directory typing for A5.
- [ ] 3.4 Decode recent comments as flat root/reply records, preserve identity/content, and group only from wire evidence for A6.
- [ ] 3.5 Parse exact root/reply cursor stderr forms and preserve pagination/folding/compact controls for A7 and C20.
- [ ] 3.6 Remove unsupported attachment output flags; decode upload metadata and download `{id, filename, path, size}` objects; fix path/bytes cleanup for A8–A9 and D15.
- [ ] 3.7 Correct squad member add/remove positional and flag mappings, member type, role, result shapes, and exact argv regressions for A10 and C24.
- [ ] 3.8 Run focused public-operation unit/component/type checks for all A1–A10 regressions before adding new families.

## 4. Add shared input, process, and model primitives

- [ ] 4.1 Add the minimal safe content input/materialization path for inline text/bytes, files, and caller-owned streams with exact stdin/file argv, preview safety, cleanup, and secret redaction.
- [ ] 4.2 Add a frozen daemon launch-options model covering reviewed foreground, identity, paths, polling, heartbeat, watchdog, concurrency, update, and reload controls with explicit presence.
- [ ] 4.3 Add the smallest terminal-capable command/executor path required by interactive setup and token-prompt login while preserving semaphore, timeout, cancellation, redaction, and existing buffered/spawn behavior.
- [ ] 4.4 Add reusable wire-presence helpers only where current `msgspec` decoding cannot distinguish omitted, null, and falsey fields; do not introduce a generic resource framework.

## 5. Implement missing resource operations

- [ ] 5.1 Add native repository checkout with URL/ref/fresh inputs and the actual emitted checkout result, delegating registration and worktree behavior entirely to the CLI (B1).
- [ ] 5.2 Add agent env get/set and atomic skills add with dedicated secret-safe results and exact reviewed channels (B2–B4).
- [ ] 5.3 Add workspace create/update/member invite using existing workspace entities, identity resolution, presence rules, and canonical command plans (B5–B7).
- [ ] 5.4 Add squad create/update/delete/member set-role/activity with reviewed member identity, outcome, role, acknowledgement, and invalidation behavior (B8–B12).
- [ ] 5.5 Add issue timeline with the actual merged activity envelope, filters, pagination, nested projections, and bound issue access (B13).
- [ ] 5.6 Add autopilot trigger URL rotation with explicit confirmation, secret-safe result handling, invalidation, and canonical tests (B14).
- [ ] 5.7 Add chat history and thread resources/models with actual output envelopes, optional thread identity, ordering, pagination, and client wiring (B15).
- [ ] 5.8 Add the complete issue wakeup resource/model family with events/list/get/create/update/disable, complete-replacement update, re-enable, loop guards, and bounded busy retry (B16).
- [ ] 5.9 Add runtime profile list/create/update/delete/set-path/unset-path with runtime-type/protocol-family compatibility, path validation, enable presence, and client wiring (B17).

## 6. Complete typed inputs on existing operations

- [ ] 6.1 Extend agent list/create/update for archived filtering, instructions, runtime/service configuration, custom args/env/MCP, permission/visibility, concurrency, status, and reviewed clear semantics (C1–C2).
- [ ] 6.2 Extend issue create/update/assign/status/search/runs for atomic status/stage/dates/position/attachments, no-start, duplicate control, limits, closed/active/sibling filters, and safe descriptions (C3–C7).
- [ ] 6.3 Extend comment add/reply/update/list for attachment, safe content, roots/summary/full/folding/compact, and exact parent/thread behavior (C8, C20, C23).
- [ ] 6.4 Extend project list/create/update and project-resource add/update for filters, icon/lead/dates/status/repositories, all resource variants, labels, execution mode, position, and clear behavior (C9–C12).
- [ ] 6.5 Extend autopilot list/get for status and explicit secret display with default-safe redaction (C13–C14).
- [ ] 6.6 Extend skill import/create/update and skill-file upsert for local file/conflict/config/content channels and concrete flag mappings (C15, C22–C23).
- [ ] 6.7 Extend daemon start/restart/disk-usage/logs and self-update for launch options, scoped reports, line count, and distinct download timeout (C16–C19).
- [ ] 6.8 Extend login and user-profile update for callback host, prompt-token mode, human-local safeguards, and safe content without bypassing upstream guards (C21, C23).
- [ ] 6.9 Reconcile all global/inherited flags and prove each typed equivalent or presentation-only disposition without duplicating domain methods.

## 7. Preserve complete response shapes

- [ ] 7.1 Implement daemon status/disk-usage public variants and nested task/workspace/cache/aggregate records with all reviewed fields and presence rules (D1–D2).
- [ ] 7.2 Expand runtime definition, usage, and update results for identity, ownership, provider/profile/device metadata, priced/unpriced usage, targets, and timestamps (D3–D5).
- [ ] 7.3 Expand workspace/member and squad/member list/detail records while preserving intentional projection differences and absence semantics (D6–D9).
- [ ] 7.4 Expand project and project-resource records, including top-level versus embedded labels and typed reference variants (D10–D11).
- [ ] 7.5 Expand autopilot trigger and property results for redacted/secret variants, provider/filter metadata, and structured display values (D12–D13).
- [ ] 7.6 Expand Agent for every reviewed emitted metadata/redaction field while keeping raw custom environment values exclusive to the audited env operation (D14).
- [ ] 7.7 Correct version aliases/metadata and Comment variants for author/type/resolution/reactions/attachments/folding/trigger/supplement fields without losing root/reply identity (D16–D17).
- [ ] 7.8 Audit all remaining emitted acknowledgements and nested fields discovered by inventory, adding typed public projections or proven natural scalar/process results.

## 8. Close verification, documentation, and delivery

- [ ] 8.1 Expand frozen `OperationCase` and decoder tables so every typed public method has one canonical exact argv/transport/result case plus required noncanonical presence and failure rows.
- [ ] 8.2 Add source-linked native payload/stderr fixtures for every corrected or new response and negative fixtures proving malformed data fails without silent defaults.
- [ ] 8.3 Extend component fake-CLI coverage across JSON, text, bytes, stdin/file, spawn, streaming, foreground, terminal-safe, redaction, cleanup, and cancellation paths.
- [ ] 8.4 Close public method, contract inventory, generated-binding, response field, docs, `__all__`, bound surface, package artifact, and exclusion bijections with no allowlist for unknown public items.
- [ ] 8.5 Update README, API, CLI coverage, compatibility, service usage, migration, changelog, and examples with corrected signatures/results, coverage dimensions, secret/interactive safety, and breaking migrations.
- [ ] 8.6 Run focused tests after each resource slice, then Ruff check/format, mypy source/tests/scripts/tools, full non-live pytest with coverage, and live-node collection exclusion.
- [ ] 8.7 Run strict OpenSpec validation, pinned-source approved-contract validation, two-render determinism, generated freshness, source-link/content audits, build, wheel/sdist package tests, and dependency checks.
- [ ] 8.8 Verify one exact clean delivery SHA, record optional prepared-live status separately, and confirm no downloaded binary, source checkout, evidence, transient report, secret, or visual HTML is tracked or packaged.
