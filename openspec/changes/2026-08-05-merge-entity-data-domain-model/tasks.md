## 1. Shared `_BoundEntity` helper and serialization

- [x] 1.1 Create `src/multica_py/models/_bound.py` with a private `_BoundEntity(msgspec.Struct, frozen=True, kw_only=True)` mixin: a `_client: MulticaClient | None = msgspec.field(default=None, name="_client")` field, a `_require_client(*, entity_type: str, entity_id: str, relation_name: str) -> MulticaClient` method raising `DetachedEntityError` when `_client is None`, and a `detach() -> Self` method returning `msgspec.structs.replace(self, _client=None)` (subclasses clear caches via override). Add a class-level `_PUBLIC_FIELDS: tuple[str, ...]` protocol (each subclass declares it) used by the equality/repr/serialization helpers. No `Any`. Module is private (`_bound.py`, leading underscore); not exported from `multica_py`.
- [x] 1.2 On `_BoundEntity`, implement `__eq__`, `__hash__`, and `__repr__` comparing/hashing/printing only the `_PUBLIC_FIELDS` (exclude `_client` and lazy caches). Implement `to_dict() -> dict[str, object]`, `from_dict(cls, data) -> Self` (classmethod), `to_json() -> str`, `from_json(cls, payload: str | bytes) -> Self` (classmethod) using `msgspec.json` / `msgspec.structs.to_builtins` scoped to `_PUBLIC_FIELDS`. `from_dict`/`from_json` construct with `_client=None`.
- [x] 1.3 Add unit tests in `tests/unit/resources/test_bound_entity.py` (reuse `mock_transport` from `tests/unit/resources/conftest.py`) covering: two instances with different `_client` but same public fields compare equal; `to_json`/`to_dict` exclude `_client` and cache fields; `from_json(payload) == detach()`; `__repr__` excludes `_client`. Use one representative unified class (once `Issue` exists in task 2; this task's test lands after task 2.5).
- [x] 1.4 Run `uv run mypy src` and `uv run mypy tests`; fix until green. No `Any` leaks.

## 2. `Issue` unified class (most entangled)

- [x] 2.1 In `src/multica_py/resources/issues.py`, rename `class IssueEntity(ResourceEntity[IssueData])` to `class Issue(_BoundEntity)`. Move the public fields from `models.issues.IssueData` onto `Issue` as frozen `msgspec.Struct` fields (`id`, `title`, `description`, `status`, `priority`, `assignee`, `pull_request_snapshot`, `child_stages`, `label_names`, `metadata_snapshot`, `attachments`, `created_at`, `updated_at`, `parent_id`, `project_id`, `creator_id`, `creator_type`). The legacy `IssueData.pull_requests` snapshot is named `pull_request_snapshot` because R26 reserves `Issue.pull_requests` for its lazy relation; this mapping is recorded in `docs/migration.md`. Add private cache fields `_comments`, `_recent_threads`, `_labels`, `_subscribers`, `_metadata`, `_pull_requests`, `_children`, `_runs` as `msgspec.field(default=None, name="...")`. `_recent_threads` is a mutable memo dict: declare it as `_recent_threads: dict[...] = msgspec.field(default_factory=dict, name="_recent_threads")` and update it by mutating the dict in place (the frozen struct holds the dict reference; the dict's contents are mutable). Set `_PUBLIC_FIELDS` to the public field tuple. Keep the lazy-relation loaders and mutation methods unchanged in behavior; replace `self._data.id` with `self.id` and `self._data.<field>` with `self.<field>` throughout. Replace `self._check_client(...)` calls with `self._require_client(entity_type="Issue", entity_id=self.id, relation_name=...)`.
- [x] 2.2 Remove `models.issues.IssueData` and the passive `models.issues.Issue` DTO. Update `IssueChildrenResult.children` and `.unstaged` to `tuple[Issue, ...]`. Keep `IssueSummary`, `IssueListFilter`, `IssueCreateRequest`, `IssueUpdateRequest`, `IssueAssignmentRequest`, `IssueReorderRequest`, `IssueListPage`, `IssueMetadataItem`, `IssueAssignee`, `LinkedPullRequest`, `IssueChildStageGroup` unchanged. Remove `_issue_data_from_issue`. Update `IssueResource._bind_issue` to construct `Issue` directly (no `IssueData`).
- [x] 2.3 In `src/multica_py/_internal/wire_models.py`, rename `IssueWire` to `_IssueWire`, `IssueSummaryWire` to `_IssueSummaryWire`, `IssueListPageWire` to `_IssueListPageWire`, `IssueChildrenResultWire` to `_IssueChildrenResultWire`, `IssuePullRequestsResultWire` to `_IssuePullRequestsResultWire`. Replace `issue_data_from_wire` with `_issue_from_wire(wire: _IssueWire) -> Issue` constructing the unified `Issue` directly. Remove `issue_from_wire` (the passive-DTO variant). Update `issue_children_result_from_wire` to build `Issue` directly. Update all callers in `resources/issues.py` to the new names.
- [x] 2.4 Update `src/multica_py/models/relations.py`: change `RelationMetadata.unstaged: tuple[IssueEntity, ...]` to `tuple[Issue, ...]` under the `TYPE_CHECKING` import (rename the import from `IssueEntity` to `Issue`). Keep the runtime type as `tuple[object, ...]` via the `TYPE_CHECKING` guard as today.
- [x] 2.5 Update `tests/cases/operations.py` `assert_result` closures referencing `IssueEntity`/`IssueData` to `Issue`. Update the 5 special-case branches in `tests/cases/operations.py::assert_type` (the `expected/actual` module-path equivalence block, ~lines 240-261) to the unified module paths; delete any branch that becomes `expected == actual` (e.g. once `multica_py.models.issues.Issue` is removed and the unified class is `multica_py.resources.issues.Issue`, flip each branch's `actual` comparison to the unified path and drop branches where `expected` already equals the unified `actual`). Update `tests/contract/test_bound_public_surface.py`: in `PUBLIC_EXPORT_CASES` replace `("Issue", IssueEntity)` keeps `("Issue", Issue)` (now the unified class), remove `("IssueData", IssueData)`; update `UNSUPPORTED_RELATION_CASES`, `SINGULAR_RELATION_CASES`, and `_consumer_type_examples` to unified names. Update `tests/contract/test_public_invariants.py` resolution namespace `IssueEntity -> Issue`. Update `tests/unit/resources/test_issues.py` and `test_issue_relations.py` imports to `Issue`. Update `tests/contract/test_bound_public_docs.py` `MIGRATION_CASES`/`EXAMPLE_CASES` for the Issue rename.
- [x] 2.6 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 3. `Project` unified class

- [x] 3.1 In `src/multica_py/resources/projects.py`, the `Project` class is already named `Project` but extends `ResourceEntity[ProjectData]`. Change it to extend `_BoundEntity`, absorb `ProjectData` fields (`id`, `name`, `description`, `status`) onto `Project`, add `_resources` and `_issues` private cache fields, set `_PUBLIC_FIELDS`. Replace `self._data.<field>` with `self.<field>`. Remove the `Project as ProjectRecord` import alias and the `_bind_project` round-trip through `ProjectData`; construct `Project` directly from `_ProjectWire` via `_project_from_wire`.
- [x] 3.2 Remove `models.projects.ProjectData` and the passive `models.projects.Project` DTO. Update `tests/cases/operations.py` (including the `assert_type` `expected/actual` equivalence branch for `multica_py.models.projects.Project` — flip `actual` to the unified path; delete the branch if it becomes `expected == actual`) and `tests/contract/test_bound_public_surface.py` (`PUBLIC_EXPORT_CASES` drop `ProjectData`). Update `tests/unit/resources/test_project_relations.py` imports.
- [x] 3.3 In `src/multica_py/_internal/wire_models.py`, rename `ProjectWire` to `_ProjectWire`; `_project_from_wire` constructs the unified `Project` directly.
- [x] 3.4 Update `src/multica_py/resources/workspaces.py` `Workspace.projects` loader: it currently round-trips through `ProjectData`/`Project` isinstance checks; replace with direct `Project` construction from `client.projects.list()`.
- [x] 3.5 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 4. `Agent` unified class

- [x] 4.1 In `src/multica_py/resources/agents.py`, rename `class AgentEntity(ResourceEntity[AgentData])` to `class Agent(_BoundEntity)`. Absorb `AgentData` fields (`id`, `name`, `description`, `skill_refs`, `archived_at`) onto `Agent`. Add `_skills`, `_tasks`, `_issues` private cache fields. Set `_PUBLIC_FIELDS`. Replace `self._data.<field>` with `self.<field>`. Replace `_bind_agent`'s `AgentData(...)` construction with direct `Agent(...)` construction from the decoded `Agent` (which is now the unified class). Keep `AgentResource.list/get/create/update` return types as `Agent`.
- [x] 4.2 Remove `models.agents.AgentData` and the passive `models.agents.Agent` DTO. Update `tests/cases/operations.py` (the `assert_type` block has no `Agent` equivalence branch — Agent decodes directly today — so only the `assert_result` closures change) and `tests/contract/test_bound_public_surface.py` (`AgentEntity -> Agent`, drop `AgentData`). Update `tests/unit/resources/test_agent_skill_squad_relations.py` imports. Update `tests/contract/test_bound_public_docs.py` `AgentData.skill_refs -> Agent.skill_refs`.
- [x] 4.3 Agent decodes directly to the public model today (no wire model); keep that — `_run_json_decode(..., Agent)` now decodes into the unified `Agent`.
- [x] 4.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 5. `Skill` unified class

- [x] 5.1 In `src/multica_py/resources/skills.py`, rename `class SkillEntity(ResourceEntity[SkillData])` to `class Skill(_BoundEntity)`. Absorb `SkillData` fields (`id`, `name`, `description`, `file_count`) onto `Skill`. Add `_files` private cache. Set `_PUBLIC_FIELDS`. Replace `_bind_skill`'s `SkillData(...)` with direct `Skill(...)`.
- [x] 5.2 Remove `models.skills.SkillData` and the passive `models.skills.Skill` DTO. Update `tests/cases/operations.py` (no `assert_type` equivalence branch for Skill — decodes directly; only `assert_result` closures change), `tests/contract/test_bound_public_surface.py` (`SkillEntity -> Skill`, drop `SkillData`), and `tests/contract/test_bound_public_docs.py`.
- [x] 5.3 Skill decodes directly today; keep `_run_json_decode(..., Skill)` decoding into the unified `Skill`.
- [x] 5.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 6. `Autopilot` and `AutopilotRun` unified classes

- [x] 6.1 In `src/multica_py/resources/autopilots.py`, rename `class AutopilotEntity(ResourceEntity[AutopilotData])` to `class Autopilot(_BoundEntity)` and `class AutopilotRunEntity(ResourceEntity[AutopilotRunData])` to `class AutopilotRun(_BoundEntity)`. Absorb `AutopilotData` fields onto `Autopilot` and `AutopilotRunData` fields onto `AutopilotRun`. Add private cache fields (`_triggers`, `_subscribers`, `_runs` on `Autopilot`; `_messages` on `AutopilotRun`). Set `_PUBLIC_FIELDS` on each. Preserve the `triggers=`/`subscribers=` seed kwargs on `Autopilot.__init__` via `__post_init__` + `object.__setattr__` (declare `triggers`/`subscribers` as kw-only fields defaulting to `msgspec.UNSET`; in `__post_init__`, when a value is not `msgspec.UNSET`, build the seeded `LazyCollection` with `object.__setattr__`; do not hand-write `__init__`). Replace `self._data.<field>` with `self.<field>`.
- [x] 6.2 Remove `models.autopilots.AutopilotData` and `models.autopilots.AutopilotRunData` and the passive `models.autopilots.Autopilot` / `AutopilotRun` DTOs. Keep `AutopilotListPage`, `AutopilotRunListPage`, `AutopilotSubscriber`, `AutopilotTrigger`, `TriggerConfigItem`, `AutopilotTriggerCreate`, `AutopilotTriggerUpdate` unchanged.
- [x] 6.3 In `src/multica_py/_internal/wire_models.py`, rename `AutopilotWire` to `_AutopilotWire`, `AutopilotRunWire` to `_AutopilotRunWire`, `AutopilotListWire` to `_AutopilotListWire`, `AutopilotRunListPageWire` to `_AutopilotRunListPageWire`, `AutopilotGetWire` to `_AutopilotGetWire`, `AutopilotSubscriberWire` to `_AutopilotSubscriberWire`, `AutopilotTriggerWire` to `_AutopilotTriggerWire`. Replace `autopilot_data_from_wire` with `_autopilot_from_wire(wire) -> Autopilot` constructing the unified class. Remove `_autopilot_data_from_model`, `autopilot_run_data_from_model`, `autopilot_get_from_wire`'s `AutopilotGetResult.data: AutopilotData` (change to `Autopilot`). Update `AutopilotResource._bind_autopilot`/`_bind_run` to construct `Autopilot`/`AutopilotRun` directly.
- [x] 6.4 Update `tests/cases/operations.py` (update the `assert_type` equivalence branches for `multica_py.models.autopilots.Autopilot` and `multica_py.models.autopilots.AutopilotRun` — flip `actual` to the unified paths; delete any branch that becomes `expected == actual`), `tests/contract/test_bound_public_surface.py` (`AutopilotEntity -> Autopilot`, `AutopilotRunEntity -> AutopilotRun`, drop `AutopilotData`/`AutopilotRunData`), `tests/unit/resources/test_autopilot_relations.py` imports, and `tests/contract/test_bound_public_docs.py`.
- [x] 6.5 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 7. `Squad` and `WorkspaceMember` unified classes

- [x] 7.1 In `src/multica_py/resources/squads.py`, rename `class SquadEntity(ResourceEntity[SquadData])` to `class Squad(_BoundEntity)`. Absorb `SquadData` fields (`id`, `name`, `member_count`, `leader_id`, `archived_at`) onto `Squad`. Add `_members`, `_issues` private caches. Set `_PUBLIC_FIELDS`. Replace `_bind_squad`'s `SquadData(...)` with direct `Squad(...)`.
- [x] 7.2 In `src/multica_py/resources/workspaces.py`, rename `class WorkspaceMemberEntity(ResourceEntity[WorkspaceMemberData])` to `class WorkspaceMember(_BoundEntity)`. Absorb `WorkspaceMemberData` fields (`id`, `name`, `role`, `user_id`, `email`) onto `WorkspaceMember`. Add `_issues` private cache. Set `_PUBLIC_FIELDS`. Replace `_bind_workspace_member` to construct `WorkspaceMember` directly (no `WorkspaceMemberData`).
- [x] 7.3 Remove `models.system.SquadData` and `models.system.WorkspaceMemberData` and the passive `models.system.Squad` / `models.workspaces.WorkspaceMember` DTOs. Update `tests/cases/operations.py` (no `assert_type` equivalence branches for Squad/WorkspaceMember — they decode directly; only `assert_result` closures change), `tests/contract/test_bound_public_surface.py` (`SquadEntity -> Squad`, `WorkspaceMemberEntity -> WorkspaceMember`, drop `SquadData`/`WorkspaceMemberData`), and `tests/contract/test_bound_public_docs.py`.
- [x] 7.4 Squad and WorkspaceMember decode directly today; keep that.
- [x] 7.5 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 8. `Workspace` unified class

- [x] 8.1 In `src/multica_py/resources/workspaces.py`, rename `class WorkspaceEntity(ResourceEntity[WorkspaceData])` to `class Workspace(_BoundEntity)`. Absorb `WorkspaceData` fields (`id`, `name`, `description`) onto `Workspace`. Add private cache fields (`_members`, `_agents`, `_skills`, `_projects`, `_labels`, `_repositories`, `_runtimes`, `_squads`, `_issues`, `_autopilots`). Set `_PUBLIC_FIELDS`. Replace `_bind_workspace` to construct `Workspace` directly (no `WorkspaceData`). Update `Workspace.projects` loader to use the unified `Project`.
- [x] 8.2 Remove `models.workspaces.WorkspaceData` and the passive `models.workspaces.Workspace` DTO. Update `tests/cases/operations.py` (no `assert_type` equivalence branch for Workspace — decodes directly; only `assert_result` closures change), `tests/contract/test_bound_public_surface.py` (`WorkspaceEntity -> Workspace`, drop `WorkspaceData`), `tests/unit/resources/test_workspace_relations.py` and `test_workspace_relations_extra.py` imports, and `tests/contract/test_bound_public_docs.py`.
- [x] 8.3 Workspace decodes directly today; keep that.
- [x] 8.4 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 9. `Comment`, `CommentThread`, `TaskRun`, `Label` unified classes

- [x] 9.1 In `src/multica_py/resources/issue_comments.py`, `Comment` and `CommentThread` already bear the canonical names and extend `ResourceEntity[CommentData]`/`ResourceEntity[CommentThreadData]`. Change them to extend `_BoundEntity`, absorb `CommentData`/`CommentThreadData` fields onto the unified classes, add private cache fields (`_comments` on `CommentThread`; `Comment` has no caches), set `_PUBLIC_FIELDS`. Remove `_comment_data` / `_thread_data` round-trip helpers; `_bind_comment` / `_bind_thread` construct `Comment`/`CommentThread` directly from the wire model. Keep the `issue_id` kwarg on `CommentThread.__init__`.
- [x] 9.2 In `src/multica_py/resources/issues.py`, `TaskRun` extends `ResourceEntity[TaskRunData]`. Change it to extend `_BoundEntity`, absorb `TaskRunData` fields onto `TaskRun`, add `_messages` private cache and keep the `_issue_id` private field, set `_PUBLIC_FIELDS`. Update `IssueResource.runs` and `IssueEntity.runs` (now `Issue.runs`) to construct `TaskRun` directly.
- [x] 9.3 In `src/multica_py/resources/labels.py`, `Label` extends `ResourceEntity[LabelData]`. Change it to extend `_BoundEntity`, absorb `LabelData` fields (`id`, `name`, `color`) onto `Label`, set `_PUBLIC_FIELDS` (Label has no lazy caches). Update `LabelResource._bind` to construct `Label` directly (no `LabelData`).
- [x] 9.4 Remove `models.issue_activity.CommentData`, `models.issue_activity.CommentThreadData`, `models.issue_activity.TaskRunData`, `models.labels.LabelData`, and the passive `models.issue_activity.Comment`/`CommentThread`/`TaskRun` DTOs. Update `tests/cases/operations.py` (update the `assert_type` equivalence branch for `multica_py.models.issue_activity.Comment` — flip `actual` to the unified path `multica_py.resources.issue_comments.Comment`; note the branch already has `actual == "multica_py.resources.issue_comments.Comment"`, so once `expected` becomes the unified path too the branch collapses to `expected == actual` and is deleted; no equivalence branches exist for `CommentThread`/`TaskRun`/`Label`), `tests/contract/test_bound_public_surface.py` (drop `CommentData`/`CommentThreadData`/`TaskRunData`/`LabelData`), and `tests/contract/test_bound_public_docs.py`. Note: `LabelData` is referenced inside `_IssueWire`/`_IssueSummaryWire` for the `labels: tuple[LabelData, ...]` field — replace with a private `_LabelWire` struct in `_internal/wire_models.py` (or keep `LabelData` as a private wire-only struct renamed `_LabelWire`) and normalize to `label_names: tuple[str, ...]` in the from-wire helper. This is the one case where a `*Data` survives as a private wire shape.
- [x] 9.5 In `src/multica_py/_internal/wire_models.py`, rename `CommentWire` to `_CommentWire`, `CommentThreadWire` to `_CommentThreadWire`. `comment_from_wire`/`comment_thread_from_wire` construct the unified `Comment`/`CommentThread` directly.
- [x] 9.6 Run `uv run pytest -m "not live" tests/unit tests/contract`, `uv run mypy src`, `uv run mypy tests`; fix until green.

## 10. Remove `ResourceEntity` and clean `models/__init__.py`

- [x] 10.1 Remove the `ResourceEntity` class from `src/multica_py/models/__init__.py`. Remove the `TData` TypeVar and the `from_data`/`to_data` methods. Update `models/__init__.py` `__all__` to drop `ResourceEntity`, `AgentData`, `AutopilotData`, `AutopilotRunData`, `CommentData`, `CommentThreadData`, `IssueData`, `LabelData`, `ProjectData`, `ProjectResourceData`, `SkillData`, `SquadData`, `TaskRunData`, `WorkspaceData`, `WorkspaceMemberData`. Keep `Page`, `ActionResult`, the relation types, `IssueChildrenResult`, the request/filter models, `LocalDirectoryResourceRef`, `ProjectResourceRecord`, `ProjectResourceAddLocalDirectoryRequest`, `ProjectResourceUpdateLocalDirectoryRequest`, `AutopilotTriggerCreate`, `AutopilotTriggerUpdate`.
- [x] 10.2 Grep `src/multica_py/` for any remaining `ResourceEntity` import and remove/replace with `_BoundEntity`. Grep for any remaining `*Data` import in `src/` and remove (the `LabelData`-as-wire case from task 9.4 is the only survivor, and it lives in `_internal/wire_models.py` as `_LabelWire`).
- [x] 10.3 Confirm `tests/contract/test_bound_public_surface.py::test_models_package_exports_only_non_runtime_relation_types` and `test_relation_module_exports_only_public_relation_types` still pass.

## 11. Public exports and docs

- [x] 11.1 Update `src/multica_py/__init__.py`: remove the dropped `*Data`/`*Entity`/`ResourceEntity` imports and `__all__` entries; replace the `as` aliases (`AgentEntity as Agent`, `IssueEntity as Issue`, `SkillEntity as Skill`, `SquadEntity as Squad`, `WorkspaceEntity as Workspace`, `WorkspaceMemberEntity as WorkspaceMember`, `AutopilotEntity as Autopilot`, `AutopilotRunEntity as AutopilotRun`) with direct imports of the unified classes from their resource modules. Keep `Comment`, `CommentThread`, `Label`, `TaskRun`, `Project` direct imports. Keep all enums, exceptions, request/filter models, relation types, `MulticaClient`, `ClientConfig`, `ManagedProcess`, `Unset`, `IssueSummary`, `IssueChildrenResult`, `ProjectResourceRecord`, `LocalDirectoryResourceRef`.
- [x] 11.2 Update `docs/migration.md`: add a rename table mapping each removed `*Data`/`*Entity`/passive-DTO name to the unified class name; replace the `to_data()` example with `to_json()` / `to_dict()`; add a note that the unified class carries private runtime state and no `IssueData = Issue` alias is shipped. Update the existing `IssueData.label_names`/`IssueData.child_stages`/`IssueData.metadata_snapshot`/`AutopilotData.subscriber_snapshot` rows to the unified-class field names. Update `IssueEntity.attachments` -> `Issue.attachments`.
- [x] 11.3 Update `docs/api.md`: replace the `ResourceEntity[TData]` line with a one-line description of the unified domain class + `_BoundEntity` private helper. Update `to_data()` references to `to_json()`/`to_dict()`.
- [x] 11.4 Update `docs/service-usage.md`: replace `issue.to_data()` with `issue.to_json()` (or `to_dict()`); update any `IssueData`/`AgentData`/... references to the unified class. Update `examples/resource_relations.py`: `issue.to_data` -> `issue.to_json` (or `to_dict`). Update `examples/issue_queue.py` and `examples/issue_workflow.py` if they reference `*Data`/`*Entity`.
- [x] 11.5 Update `tests/contract/test_bound_public_docs.py` `MIGRATION_CASES`/`EXAMPLE_CASES`/`CONSUMER_DOCUMENTATION_CASES` to match the new doc strings (the test pins the doc content). Re-run after docs land.

## 12. Final verification

- [x] 12.1 Run `uv run pytest -m "not live"` (full offline suite) end-to-end and confirm green; verify with `uv run pytest -m "not live" --collect-only` that no `tests/live/*` node is collected.
- [x] 12.2 Run `uv run mypy src` and `uv run mypy tests` end-to-end; confirm no `Any` leaks and no new errors.
- [x] 12.3 Run `uv run ruff check` and `uv run ruff format --check`; fix until green.
- [x] 12.4 Run `openspec validate 2026-08-05-merge-entity-data-domain-model` and `openspec validate --specs`; fix any reported drift.
- [x] 12.5 Re-run `tests/unit/resources/test_operations.py::test_discovered_public_methods` and confirm it stays green (the discovered set equals the canonical set; no canonical `sdk_method` is renamed). Also assert `len({c.id for c in OPERATION_CASES}) == len(OPERATION_CASES)` is unchanged (no case added/removed); do not pin a numeric canonical/case count.
